<%= link_to gravatar_for(@user, size: 50), @user %>
<h1><%= @user.name %></h1>
<p><%= get_rank_name(@user.rank) %></p>
<p><%= get_title_of_honor(@user.title_of_honor) %></p>
<span><%= link_to "プロフィールを見る", @user %></span>
<span><%= pluralize(@user.microposts.count, "投稿") %></span>