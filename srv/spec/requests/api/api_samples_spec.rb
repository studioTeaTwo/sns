require 'rails_helper'

RSpec.describe "Api::Samples", type: :request do
  describe "GET /api_samples" do
    it "works! (now write some real specs)" do
      get api_samples_path
      #expect(response).to have_http_status(200)
    end
  end
end
