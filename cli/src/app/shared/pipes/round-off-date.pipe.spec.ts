import { RoundOffDatePipe } from './round-off-date.pipe';

describe('RoundOffDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundOffDatePipe();
    expect(pipe).toBeTruthy();
  });
});
