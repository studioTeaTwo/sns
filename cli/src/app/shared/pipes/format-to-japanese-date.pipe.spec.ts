import { FormatToJapaneseDatePipe } from './format-to-japanese-date.pipe';

describe('FormatToJapaneseDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatToJapaneseDatePipe();
    expect(pipe).toBeTruthy();
  });
});
